.hero__bottom-btn{
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    padding-bottom: 117px;
}

.hero__content{
    position: absolute;
    top:50%;
    left: 50%;
    transform: translate(-50%, -50%);
}

.hero{
    height: 100vh;
    position: relative;
    min-height: 650px;

}

